apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  namespace: app
type: Opaque
stringData:
  # ═══════════════════════════════════════════════════════════════
  # 📝 NOTE: This Nginx app doesn't actually need database credentials!
  # ═══════════════════════════════════════════════════════════════
  # For PRODUCTION deployment:
  #   1. Use: kubectl create secret (see scripts/create-secrets.sh)
  #   2. Or use: secret.yaml.template with CI/CD
  #   3. Or use: HashiCorp Vault (see docs/SECRET-MANAGEMENT.md)
  # ═══════════════════════════════════════════════════════════════
  
  # Application banner (displayed in app)
  APP_BANNER: "GlobalLogic"

